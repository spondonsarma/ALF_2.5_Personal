FC= ifort
#FC= mpxlf90
#FLAGS= -c  -q64  -O4 
#FLAGS= -c   -O1  -pg
FLAGS= -c   -O3 
SUFFIX= -qsuffix=f=f90
LF= -warn all 
LIB=modules_90.a

all: 
	(make -f Compile FC="$(FC)" FLAGS="$(FLAGS)"  LIB="$(LIB)" )

clean:
	(make -f Compile  clean ) ;\
 	rm *.mod
